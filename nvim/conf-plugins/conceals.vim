" disables conceal for some filetypes, which hides e.g. markdown code block marker character (`), double quotes on json files, etc...

let g:vim_markdown_conceal = 0
let g:vim_markdown_conceal_code_blocks = 0

let g:vim_json_conceal = 0
